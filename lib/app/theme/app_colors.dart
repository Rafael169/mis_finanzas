import 'package:flutter/material.dart';

/// Colores de marca y del semáforo de presupuestos.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF1E6FD9);
  static const Color background = Color(0xFFF4F7FB);

  // Ingresos y gastos
  static const Color income = Color(0xFF2E9E5B);
  static const Color expense = Color(0xFFD64545);

  // Semáforo de ejecución del presupuesto
  static const Color ok = Color(0xFF2E9E5B);
  static const Color warning = Color(0xFFF2A900);
  static const Color over = Color(0xFFD64545);
}